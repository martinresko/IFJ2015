int factorial();

int jano ()
{
int a;
if (a < 0) 
{
	cout << "Faktorial nelze spocitat!\n";
} 
else 
{
	int vysl = 0;
	vysl = factorial(a);
	int neg = 0 - vysl;
	cout << "Vysledek: " << vysl << " (zaporny: " << neg << ")\n";
}
//return 0;
}

